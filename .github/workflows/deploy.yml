name: deploy
# Skeleton — wire to your cloud target (AKS / EKS / GCR + Cloud Run, etc.)
on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO wire to terraform apply + helm upgrade"
