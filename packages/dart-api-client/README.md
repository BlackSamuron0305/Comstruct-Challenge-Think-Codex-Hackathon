# Dart API Client (placeholder)

OpenAPI-generated client for the Flutter mobile app.

Regenerate via:

```
make api-types
```

Generation pulls the gateway's OpenAPI spec at `http://localhost:3000/openapi.json` and writes to `lib/src/generated/`. Until the gateway is running, this package contains hand-rolled stubs in `lib/comstruct_api.dart`.
