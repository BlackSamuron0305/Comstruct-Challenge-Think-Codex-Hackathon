export function UsersPage(): JSX.Element {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-3">Users & roles</h1>
      <p className="text-sm text-slate-500">
        Demo build — user management UI is stubbed. The seed loads three demo users
        (foreman, project manager, procurement admin).
      </p>
    </div>
  );
}
